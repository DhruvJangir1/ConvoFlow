{
  "buildCommand": "npx prisma generate --schema=prisma/schema.prisma && npx tsc -b --noEmit && npx vite build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://convoflow-2.onrender.com/api/:path*"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
